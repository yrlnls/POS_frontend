import { Typography } from '@mui/material';

export default function SalesDashboard() {
  return (
    <div>
      <Typography variant="h4">Sales Dashboard</Typography>
      <Typography>This is the sales dashboard content</Typography>
    </div>
  );
}