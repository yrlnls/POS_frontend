import { Typography } from '@mui/material';

export default function CustomerDashboard() {
  return (
    <div>
      <Typography variant="h4">Customer Dashboard</Typography>
      <Typography>This is the customer dashboard content</Typography>
    </div>
  );
}